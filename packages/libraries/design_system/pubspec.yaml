name: design_system
description: Shared design system - theme tokens and UI primitives.

version: 1.0.0

environment:
  sdk: ">=3.10.7 <4.0.0"
  flutter: ">=3.38.7"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

publish_to: none
